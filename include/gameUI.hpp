#pragma once

#include "logger.h"
#include "glm/glm/glm.hpp"
#include "sprite.hpp"
#include "constants.h"

class GameUI
{
	private:

		//Counter.
		//Sprite lives[MAX_LIVES];
		//Cherry[] not implemented.

	public:

		GameUI() = default;

		GameUI& operator =(const GameUI& other) = default;

		GameUI()
		{

		}
}
